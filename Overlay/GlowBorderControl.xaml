<UserControl x:Class="GlowBorder.Overlay.GlowBorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             IsHitTestVisible="False">
    <Grid Name="MainGrid">
        <!-- Layer 4: Subtle Soft Ambient Spill -->
        <Border Name="AmbientSpillBorder" BorderThickness="10" IsHitTestVisible="False" Margin="-5" Opacity="0.4">
            <Border.BorderBrush>
                <SolidColorBrush x:Name="SpillBrush" Color="#FF5865F2"/>
            </Border.BorderBrush>
        </Border>

        <!-- Layer 3: Edge Glow Gradients -->
        <!-- Top Edge Glow -->
        <Rectangle Name="TopEdge" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="50" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop x:Name="TopStop0" Offset="0" Color="#FF5865F2"/>
                    <GradientStop x:Name="TopStop1" Offset="1" Color="#005865F2"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Bottom Edge Glow -->
        <Rectangle Name="BottomEdge" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Height="50" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0.5,1" EndPoint="0.5,0">
                    <GradientStop x:Name="BottomStop0" Offset="0" Color="#FF5865F2"/>
                    <GradientStop x:Name="BottomStop1" Offset="1" Color="#005865F2"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Left Edge Glow -->
        <Rectangle Name="LeftEdge" HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="50" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop x:Name="LeftStop0" Offset="0" Color="#FF5865F2"/>
                    <GradientStop x:Name="LeftStop1" Offset="1" Color="#005865F2"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Right Edge Glow -->
        <Rectangle Name="RightEdge" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="50" IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="1,0.5" EndPoint="0,0.5">
                    <GradientStop x:Name="RightStop0" Offset="0" Color="#FF5865F2"/>
                    <GradientStop x:Name="RightStop1" Offset="1" Color="#005865F2"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Layer 2: Crisp Core Inner Border Line -->
        <Border Name="InnerBorder" BorderThickness="4" IsHitTestVisible="False" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Border.BorderBrush>
                <SolidColorBrush x:Name="InnerBorderBrush" Color="#FF5865F2"/>
            </Border.BorderBrush>
        </Border>

        <!-- Layer 1: Dynamic Animation Overlays (Sweep, Comet, Ripple) -->
        <Border Name="SweepOverlay" IsHitTestVisible="False" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Visibility="Collapsed">
            <Border.BorderBrush>
                <LinearGradientBrush x:Name="SweepGradientBrush" StartPoint="0,0" EndPoint="1,1">
                    <GradientStop x:Name="SweepStop0" Offset="0.0" Color="#00FFFFFF"/>
                    <GradientStop x:Name="SweepStop1" Offset="0.5" Color="#FF5865F2"/>
                    <GradientStop x:Name="SweepStop2" Offset="1.0" Color="#00FFFFFF"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
        </Border>

        <!-- Comet Trail Head Overlay -->
        <Border Name="CometOverlay" IsHitTestVisible="False" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Visibility="Collapsed">
            <Border.BorderBrush>
                <LinearGradientBrush x:Name="CometGradientBrush" StartPoint="0,0" EndPoint="1,0">
                    <GradientStop x:Name="CometStop0" Offset="0.0" Color="#00FFFFFF"/>
                    <GradientStop x:Name="CometStop1" Offset="0.7" Color="#80FFFFFF"/>
                    <GradientStop x:Name="CometStop2" Offset="1.0" Color="#FFFFFFFF"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
        </Border>

        <!-- Ripple Corner Radial Overlay -->
        <Border Name="RippleOverlay" IsHitTestVisible="False" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Visibility="Collapsed">
            <Border.BorderBrush>
                <RadialGradientBrush x:Name="RippleGradientBrush" Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="0.5" RadiusY="0.5">
                    <GradientStop x:Name="RippleStop0" Offset="0.0" Color="#FF5865F2"/>
                    <GradientStop x:Name="RippleStop1" Offset="0.5" Color="#805865F2"/>
                    <GradientStop x:Name="RippleStop2" Offset="1.0" Color="#005865F2"/>
                </RadialGradientBrush>
            </Border.BorderBrush>
        </Border>
    </Grid>
</UserControl>
