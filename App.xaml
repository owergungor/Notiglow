<Application x:Class="NotiGlow.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:animations="clr-namespace:NotiGlow.UI.Animations">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- Centralized Theme Brushes (Dark Theme Defaults) -->
            <SolidColorBrush x:Key="WindowBackground" Color="#141414" />
            <SolidColorBrush x:Key="SidebarBackground" Color="#181818" />
            <SolidColorBrush x:Key="CardBackground" Color="#202020" />
            <SolidColorBrush x:Key="CardBackgroundSecondary" Color="#1B1B1B" />
            <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF" />
            <SolidColorBrush x:Key="TextSecondary" Color="#B8B8B8" />
            <SolidColorBrush x:Key="TextMuted" Color="#808080" />
            <SolidColorBrush x:Key="BorderColor" Color="#383838" />
            <SolidColorBrush x:Key="DividerColor" Color="#282828" />
            <SolidColorBrush x:Key="InputBackground" Color="#1C1C1C" />
            <SolidColorBrush x:Key="AccentColor" Color="#5865F2" />
            <SolidColorBrush x:Key="ControlBackground" Color="#262626" />
            <SolidColorBrush x:Key="ControlHoverBackground" Color="#303030" />
            <SolidColorBrush x:Key="GlassOverlay" Color="#1CFFFFFF" />
            <SolidColorBrush x:Key="GlassBorder" Color="#455865F2" />
            <SolidColorBrush x:Key="TextDisabled" Color="#555555" />
            <SolidColorBrush x:Key="ControlPressedBackground" Color="#383838" />
            <SolidColorBrush x:Key="ControlDisabledBackground" Color="#181818" />
            <SolidColorBrush x:Key="InputBorder" Color="#383838" />
            <SolidColorBrush x:Key="NavActiveBackground" Color="#2E2E2E" />
            <SolidColorBrush x:Key="NavActiveHoverBackground" Color="#363636" />
            <SolidColorBrush x:Key="NavIndicatorColor" Color="#5865F2" />

            <!-- Slider Brushes (Theme Adaptable) -->
            <SolidColorBrush x:Key="SliderTrackBackground" Color="#404552" />
            <SolidColorBrush x:Key="SliderTrackHoverBackground" Color="#505668" />
            <SolidColorBrush x:Key="SliderThumbBackground" Color="#FFFFFF" />
            <SolidColorBrush x:Key="SliderThumbBorder" Color="#5865F2" />

            <!-- Global Style Overrides for Readability & Windows 11 Fluent Aesthetic -->
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            </Style>

            <Style TargetType="{x:Type ui:TextBox}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            </Style>

            <!-- Fluent Rounded ComboBoxItem Style -->
            <Style TargetType="{x:Type ComboBoxItem}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                            <Border x:Name="Bd" CornerRadius="6" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentColor}"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Rounded ComboBox Style -->
            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,0,32,0"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid x:Name="MainGrid" VerticalAlignment="Center">
                                <ToggleButton x:Name="ToggleButton" 
                                              Focusable="False" 
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              VerticalAlignment="Stretch"
                                              HorizontalAlignment="Stretch">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="Border" 
                                                    CornerRadius="8" 
                                                    Background="{DynamicResource ControlBackground}" 
                                                    BorderBrush="{DynamicResource BorderColor}" 
                                                    BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="28"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ui:SymbolIcon Grid.Column="1" Symbol="ChevronDown24" FontSize="11" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=MainGrid}"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Grid x:Name="DropDown"
                                          SnapsToDevicePixels="True"
                                          MinWidth="{Binding ActualWidth, ElementName=MainGrid}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                          Margin="0,4,0,4">
                                        <Border x:Name="DropDownBorder"
                                                CornerRadius="10"
                                                Background="{DynamicResource CardBackground}"
                                                BorderBrush="{DynamicResource BorderColor}"
                                                BorderThickness="1"
                                                Padding="4">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.3" Color="#000000"/>
                                            </Border.Effect>
                                            <ScrollViewer SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Theme ComboBox Style (Padding="18,8,18,8", Width=150, Height=32 vertically centered with slider & card) -->
            <Style x:Key="ThemeComboBoxStyle" TargetType="{x:Type ComboBox}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Width" Value="150"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="18,8,18,8"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid x:Name="MainGrid" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" VerticalAlignment="Center">
                                <ToggleButton x:Name="ToggleButton" 
                                              Focusable="False" 
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              VerticalAlignment="Stretch"
                                              HorizontalAlignment="Stretch">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="Border" 
                                                    CornerRadius="8" 
                                                    Background="{DynamicResource InputBackground}" 
                                                    BorderBrush="{DynamicResource BorderColor}" 
                                                    BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="28"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ui:SymbolIcon Grid.Column="1" Symbol="ChevronDown24" FontSize="11" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=MainGrid}"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Grid x:Name="DropDown"
                                          SnapsToDevicePixels="True"
                                          MinWidth="175"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                          Margin="0,4,0,4">
                                        <Border x:Name="DropDownBorder"
                                                CornerRadius="10"
                                                Background="{DynamicResource CardBackground}"
                                                BorderBrush="{DynamicResource BorderColor}"
                                                BorderThickness="1"
                                                Padding="4">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.3" Color="#000000"/>
                                            </Border.Effect>
                                            <ScrollViewer SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Modern Windows 11 Fluent Theme ToggleButton Style -->
            <Style x:Key="ThemeToggleButtonStyle" TargetType="{x:Type ToggleButton}">
                <Setter Property="animations:ButtonPressAnimationBehavior.IsEnabled" Value="True"/>
                <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="18,8,18,8"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="MinWidth" Value="75"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border x:Name="Bd"
                                    CornerRadius="7"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsChecked" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
                                </MultiTrigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Rounded Slider Style -->
            <Style TargetType="{x:Type Slider}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="MinHeight" Value="24"/>
                <Setter Property="Height" Value="24"/>
                <Setter Property="Stylus.IsPressAndHoldEnabled" Value="False"/>
                <Setter Property="Focusable" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Slider}">
                            <Grid x:Name="SliderRoot" VerticalAlignment="Center" SnapsToDevicePixels="True">
                                <!-- Inactive Track Bar (4px height, continuous full width) -->
                                <Border x:Name="TrackBackground"
                                        Height="4"
                                        CornerRadius="2"
                                        Background="{DynamicResource SliderTrackBackground}"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Stretch"
                                        IsHitTestVisible="False"/>

                                <Track x:Name="PART_Track" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}"
                                                      Focusable="False"
                                                      IsTabStop="False">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                    <Grid Background="Transparent">
                                                        <Border x:Name="ActiveTrack"
                                                                Height="4"
                                                                CornerRadius="2"
                                                                Background="{DynamicResource AccentColor}"
                                                                VerticalAlignment="Center"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                                      Focusable="False"
                                                      IsTabStop="False">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="{x:Type RepeatButton}">
                                                    <Grid Background="Transparent"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Focusable="False" SnapsToDevicePixels="True">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                    <Grid Width="20" Height="20" Background="Transparent">
                                                        <Ellipse x:Name="ThumbEllipse"
                                                                 Width="14"
                                                                 Height="14"
                                                                 Fill="{DynamicResource SliderThumbBackground}"
                                                                 Stroke="{DynamicResource SliderThumbBorder}"
                                                                 StrokeThickness="2.5"
                                                                 HorizontalAlignment="Center"
                                                                 VerticalAlignment="Center">
                                                            <Ellipse.Effect>
                                                                <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.35" Color="#000000"/>
                                                            </Ellipse.Effect>
                                                        </Ellipse>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ThumbEllipse" Property="Width" Value="16"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Height" Value="16"/>
                                                            <Setter TargetName="ThumbEllipse" Property="StrokeThickness" Value="3"/>
                                                        </Trigger>
                                                        <Trigger Property="IsDragging" Value="True">
                                                            <Setter TargetName="ThumbEllipse" Property="Width" Value="16"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Height" Value="16"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Fill" Value="{DynamicResource AccentColor}"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Stroke" Value="#FFFFFF"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="TrackBackground" Property="Background" Value="{DynamicResource SliderTrackHoverBackground}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ui:CardControl}">
                <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="18"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ui:CardControl}">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Grid x:Name="CardGrid" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Left Slot: Header (Title + Description TextBlock) -->
                                    <ContentPresenter x:Name="HeaderContent"
                                                      Grid.Column="0"
                                                      Content="{TemplateBinding Header}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Stretch"/>
                                                      
                                    <!-- Right Slot: Content (Action Control / ToggleSwitch / ComboBox) -->
                                    <ContentPresenter x:Name="BodyContent"
                                                      Grid.Column="1"
                                                      Content="{TemplateBinding Content}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Right"
                                                      Margin="20,0,0,0"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Header" Value="{x:Null}">
                                    <Setter TargetName="HeaderContent" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="BodyContent" Property="Grid.Column" Value="0"/>
                                    <Setter TargetName="BodyContent" Property="Grid.ColumnSpan" Value="2"/>
                                    <Setter TargetName="BodyContent" Property="HorizontalAlignment" Value="Stretch"/>
                                    <Setter TargetName="BodyContent" Property="Margin" Value="0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Windows 11 Task Manager Style NavigationViewItem Style -->
            <Style TargetType="{x:Type ui:NavigationViewItem}">
                <Setter Property="animations:ButtonPressAnimationBehavior.IsEnabled" Value="True"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="Margin" Value="6,2,6,2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ui:NavigationViewItem}">
                            <Grid x:Name="ItemGrid" Height="40" Background="Transparent" ClipToBounds="True">
                                <!-- Windows 11 Task Manager Style Item Surface (No Border / No Glow) -->
                                <Border x:Name="ItemBorder"
                                        CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        IsHitTestVisible="False"
                                        SnapsToDevicePixels="True">
                                    <Grid IsHitTestVisible="False">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Windows 11 Task Manager Left Slim Vertical Indicator Bar (3x24px) -->
                                        <Border x:Name="ActiveIndicator"
                                                Grid.Column="0"
                                                Width="3"
                                                Height="24"
                                                CornerRadius="1.5"
                                                Margin="3,0,0,0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource NavIndicatorColor}"
                                                Opacity="0"
                                                Visibility="Collapsed"/>

                                        <!-- Content Layout (Icon + Content Text) -->
                                        <Grid Grid.Column="1" Margin="3,0,8,0" VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="32"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Icon Presenter -->
                                            <ContentPresenter x:Name="IconPresenter"
                                                              Grid.Column="0"
                                                              Content="{TemplateBinding Icon}"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              TextElement.Foreground="{TemplateBinding Foreground}"/>

                                            <!-- Content Presenter -->
                                            <ContentPresenter x:Name="ContentPresenter"
                                                              Grid.Column="1"
                                                              Content="{TemplateBinding Content}"
                                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                                              HorizontalAlignment="Left"
                                                              VerticalAlignment="Center"
                                                              Margin="6,0,0,0"
                                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                                              TextElement.FontSize="13"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!-- Inactive Items: Muted Text & Normal Weight -->
                                <Trigger Property="IsActive" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                    <Setter Property="FontWeight" Value="Normal"/>
                                </Trigger>

                                <!-- Mouse Over Trigger (Inactive Items: Subtle Hover Highlight) -->
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsActive" Value="False"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </MultiTrigger>

                                <!-- Active (Selected) Navigation Item Trigger: Clean Elevated Gray Surface + Left Neutral Indicator -->
                                <Trigger Property="IsActive" Value="True">
                                    <Setter TargetName="ActiveIndicator" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ActiveIndicator" Property="Opacity" Value="1"/>
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource NavActiveBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>

                                <!-- Active + Mouse Over Trigger (Slightly elevated hover over active background) -->
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsActive" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ActiveIndicator" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ActiveIndicator" Property="Opacity" Value="1"/>
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource NavActiveHoverBackground}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Full-Card Selection RadioButton Style (No Bullet Circle, Ambient Card Glow Outline) -->
            <Style TargetType="{x:Type RadioButton}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <Grid x:Name="TemplateRoot" Background="Transparent" HorizontalAlignment="Stretch">
                                <!-- Ambient Glow & Border Highlight Frame -->
                                <Border x:Name="GlowBorder"
                                        CornerRadius="12"
                                        Margin="-1"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1.5"
                                        SnapsToDevicePixels="True"
                                        HorizontalAlignment="Stretch">
                                    <ContentPresenter x:Name="ContentPresenter"
                                                      Content="{TemplateBinding Content}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!-- Hover State (Unchecked) -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="GlowBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="GlowBorder" Property="Opacity" Value="0.6"/>
                                </Trigger>

                                <!-- Checked (Selected) State: Full Card Accent Outline & Ambient Glow -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="GlowBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="GlowBorder" Property="Opacity" Value="1.0"/>
                                    <Setter TargetName="GlowBorder" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.45" Color="#5865F2"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Fluent Full-Card Selection CheckBox Style (No Square Box, Ambient Card Glow Outline) -->
            <Style TargetType="{x:Type CheckBox}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid x:Name="TemplateRoot" Background="Transparent" HorizontalAlignment="Stretch">
                                <!-- Ambient Glow & Border Highlight Frame -->
                                <Border x:Name="GlowBorder"
                                        CornerRadius="12"
                                        Margin="-1"
                                        Background="Transparent"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1.5"
                                        SnapsToDevicePixels="True"
                                        HorizontalAlignment="Stretch">
                                    <ContentPresenter x:Name="ContentPresenter"
                                                      Content="{TemplateBinding Content}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!-- Hover State (Unchecked) -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="GlowBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="GlowBorder" Property="Opacity" Value="0.6"/>
                                </Trigger>

                                <!-- Checked (Selected) State: Full Card Accent Outline & Ambient Glow -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="GlowBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="GlowBorder" Property="Opacity" Value="1.0"/>
                                    <Setter TargetName="GlowBorder" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.45" Color="#5865F2"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 2:1 Aspect Ratio Fluent ToggleSwitch Style (48x24 Track) -->
            <Style TargetType="{x:Type ui:ToggleSwitch}">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="MinWidth" Value="48"/>
                <Setter Property="MinHeight" Value="24"/>
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="24"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ui:ToggleSwitch}">
                            <Grid x:Name="TemplateRoot" Width="48" Height="24" Background="Transparent">
                                <!-- Visible Track: Exactly 48x24 (2:1 Ratio) -->
                                <Border x:Name="SwitchTrack"
                                        Width="48"
                                        Height="24"
                                        CornerRadius="12"
                                        Background="{DynamicResource InputBackground}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1">
                                    <Grid x:Name="TrackGrid" Margin="2">
                                        <!-- Thumb Circle: Perfectly 18x18 Circle Vertically Centered -->
                                        <Border x:Name="SwitchThumb"
                                                Width="18"
                                                Height="18"
                                                CornerRadius="9"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource TextSecondary}"/>
                                    </Grid>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="SwitchTrack" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="SwitchThumb" Property="Background" Value="#FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
