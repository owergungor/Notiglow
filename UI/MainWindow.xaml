<ui:FluentWindow x:Class="GlowBorder.UI.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  xmlns:views="clr-namespace:GlowBorder.UI.Views"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                  mc:Ignorable="d" 
                  Title="NotiGlow 2.0" Height="760" Width="1140"
                  MinHeight="600" MinWidth="900"
                  WindowStartupLocation="CenterScreen"
                  ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
                  Background="{DynamicResource WindowBackground}">
    <Grid Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom TitleBar -->
        <ui:TitleBar Grid.Row="0" Title="NotiGlow 2.0 — Ambient Notification Utility" Icon="{ui:FontIcon &#xE714;}" Foreground="{DynamicResource TextPrimary}"/>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation View Pane -->
            <Border Grid.Column="0" Background="{DynamicResource SidebarBackground}">
                <ui:NavigationView x:Name="RootNavigationView" PaneDisplayMode="Left" SelectionChanged="RootNavigationView_SelectionChanged" Background="{DynamicResource SidebarBackground}" Foreground="{DynamicResource TextPrimary}">
                    <ui:NavigationView.Resources>
                        <Style TargetType="{x:Type ui:NavigationViewItem}">
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            <Setter Property="Margin" Value="4,2"/>
                        </Style>
                    </ui:NavigationView.Resources>
                    <ui:NavigationView.MenuItems>
                        <ui:NavigationViewItem Content="General" Tag="General" Icon="{ui:SymbolIcon Home24}" TargetPageType="{x:Type views:GeneralView}"/>
                        <ui:NavigationViewItem Content="Applications" Tag="Applications" Icon="{ui:SymbolIcon Apps24}" TargetPageType="{x:Type views:ApplicationsView}"/>
                        <ui:NavigationViewItem Content="Appearance" Tag="Appearance" Icon="{ui:SymbolIcon PaintBrush24}" TargetPageType="{x:Type views:AppearanceView}"/>
                        <ui:NavigationViewItem Content="Display" Tag="Display" Icon="{ui:SymbolIcon Desktop24}" TargetPageType="{x:Type views:DisplayView}"/>
                        <ui:NavigationViewItem Content="Gaming" Tag="Gaming" Icon="{ui:SymbolIcon Games24}" TargetPageType="{x:Type views:GamingView}"/>
                        <ui:NavigationViewItem Content="Notifications" Tag="Notifications" Icon="{ui:SymbolIcon Alert24}" TargetPageType="{x:Type views:NotificationsView}"/>
                        <ui:NavigationViewItem Content="Advanced" Tag="Advanced" Icon="{ui:SymbolIcon Wrench24}" TargetPageType="{x:Type views:AdvancedView}"/>
                    </ui:NavigationView.MenuItems>
                </ui:NavigationView>
            </Border>

            <!-- Vertical Divider -->
            <Border Grid.Column="1" Width="1" Background="{DynamicResource DividerColor}"/>

            <!-- Main Views Container -->
            <Grid Grid.Column="2" x:Name="RootContentGrid" Background="{DynamicResource WindowBackground}">
                <views:GeneralView x:Name="ViewGeneral" Visibility="Visible"/>
                <views:ApplicationsView x:Name="ViewApplications" Visibility="Collapsed"/>
                <views:AppearanceView x:Name="ViewAppearance" Visibility="Collapsed"/>
                <views:DisplayView x:Name="ViewDisplay" Visibility="Collapsed"/>
                <views:GamingView x:Name="ViewGaming" Visibility="Collapsed"/>
                <views:NotificationsView x:Name="ViewNotifications" Visibility="Collapsed"/>
                <views:AdvancedView x:Name="ViewAdvanced" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
