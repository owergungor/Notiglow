<UserControl x:Class="GlowBorder.UI.Views.ApplicationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:GlowBorder.UI.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Controls -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Tracked Applications" FontSize="26" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text="Manage custom glow colors, durations, priorities, and animation styles per application" Foreground="{DynamicResource TextSecondary}" FontSize="13" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:Button x:Name="BtnAddApplication" Appearance="Secondary" Click="BtnAddApplication_Click" Margin="0,0,10,0" VerticalAlignment="Center" ToolTip="Add a new custom application profile" Foreground="{DynamicResource TextPrimary}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Add24" Foreground="{DynamicResource TextPrimary}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add Application" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>

                <ui:Button x:Name="BtnTestAnimation" Appearance="Primary" Click="BtnTestAnimation_Click" VerticalAlignment="Center" ToolTip="Test notification glow animation" Foreground="{DynamicResource TextPrimary}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Play24" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Test Animation" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ui:Button>
            </StackPanel>
        </Grid>

        <!-- Main Content Area: App List vs Edit Drawer Overlay -->
        <Grid Grid.Row="2">
            <!-- App Profile List ScrollViewer -->
            <ScrollViewer x:Name="ListScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="PnlAppCards" HorizontalAlignment="Stretch"/>
            </ScrollViewer>

            <!-- Edit / Add Profile Dialog Drawer Overlay -->
            <Border x:Name="PnlEditProfile" Background="{DynamicResource CardBackground}" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Padding="24" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <TextBlock x:Name="TxtEditorTitle" Text="Edit App Profile" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                            <ui:Button x:Name="BtnCloseEditor" Icon="{ui:FontIcon &#xE711;}" HorizontalAlignment="Right" Appearance="Secondary" Click="BtnCloseEditor_Click"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                                <!-- App Name & ID -->
                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock Text="Application Name" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                                    <ui:TextBox x:Name="TxtEditAppName" PlaceholderText="e.g. Discord"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock Text="App Identifier / Executable Name" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                                    <ui:TextBox x:Name="TxtEditAppId" PlaceholderText="e.g. discord or discord.exe"/>
                                </StackPanel>

                                <!-- Animation Style -->
                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock Text="Animation Style" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="CmbEditStyle" SelectionChanged="EditControl_Changed" HorizontalAlignment="Stretch">
                                        <ComboBoxItem Content="Pulse (Uniform Opacity Pulse)" Tag="Pulse"/>
                                        <ComboBoxItem Content="Sweep (Moving Light Perimeter)" Tag="Sweep"/>
                                        <ComboBoxItem Content="Ambient (Soft Breathing Glow)" Tag="Ambient"/>
                                        <ComboBoxItem Content="Comet (Travelling Light Head &amp; Trail)" Tag="Comet"/>
                                        <ComboBoxItem Content="Ripple (Expanding Corner Glow Wave)" Tag="Ripple"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Notification Priority -->
                                <StackPanel Margin="0,0,0,16">
                                    <TextBlock Text="Notification Priority" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="CmbEditPriority" HorizontalAlignment="Stretch">
                                        <ComboBoxItem Content="Low Priority" Tag="Low"/>
                                        <ComboBoxItem Content="Normal Priority" Tag="Normal"/>
                                        <ComboBoxItem Content="High Priority (Bypasses Gaming Mode filter)" Tag="High"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Duration Slider -->
                                <StackPanel Margin="0,0,0,16">
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="Duration (ms)" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock x:Name="TxtDurationVal" Text="4000 ms" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondary}"/>
                                    </Grid>
                                    <Slider x:Name="SldDuration" Minimum="500" Maximum="15000" Value="4000" SmallChange="500" LargeChange="1000" TickFrequency="500" IsSnapToTickEnabled="True" ValueChanged="SldDuration_ValueChanged"/>
                                </StackPanel>

                                 <!-- Intensity Slider -->
                                <StackPanel Margin="0,0,0,16">
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="Glow Intensity" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock x:Name="TxtIntensityVal" Text="80%" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondary}"/>
                                    </Grid>
                                    <Slider x:Name="SldIntensity" Minimum="10" Maximum="100" Value="80" SmallChange="1" LargeChange="5" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SldIntensity_ValueChanged"/>
                                </StackPanel>

                                <!-- Line Thickness -->
                                <StackPanel Margin="0,0,0,16">
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="Border Line Thickness" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock x:Name="TxtThicknessVal" Text="4 px" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondary}"/>
                                    </Grid>
                                    <Slider x:Name="SldThickness" Minimum="1" Maximum="10" Value="4" SmallChange="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SldThickness_ValueChanged"/>
                                </StackPanel>

                                <!-- Glow Size -->
                                <StackPanel Margin="0,0,0,16">
                                    <Grid Margin="0,0,0,6">
                                        <TextBlock Text="Glow Blur Size" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                        <TextBlock x:Name="TxtGlowSizeVal" Text="30 px" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondary}"/>
                                    </Grid>
                                    <Slider x:Name="SldGlowSize" Minimum="5" Maximum="100" Value="30" SmallChange="1" LargeChange="5" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SldGlowSize_ValueChanged"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Top">
                                <!-- Color Picker -->
                                <controls:ColorPickerControl x:Name="EditColorPicker" ColorChanged="EditColorPicker_ColorChanged" Margin="0,0,0,16"/>

                                <!-- Live Preview Box -->
                                <TextBlock Text="Live Preview" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                                <controls:EdgePreviewControl x:Name="EditEdgePreview" Height="160" HorizontalAlignment="Stretch"/>

                                <!-- Save / Cancel Buttons -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                                    <ui:Button x:Name="BtnSaveProfile" Content="Save Profile" Appearance="Primary" Click="BtnSaveProfile_Click" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <ui:Button Content="Cancel" Appearance="Secondary" Click="BtnCloseEditor_Click" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
